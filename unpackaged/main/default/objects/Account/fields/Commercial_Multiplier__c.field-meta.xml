<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Commercial_Multiplier__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
  OR(Current_Pipeline_Amount__c &gt;250000, Committed_ARR__c &gt;250000), 2,

IF(
  OR(Current_Pipeline_Amount__c &gt;100000, Committed_ARR__c &gt;100000), 1.5,

IF(
  OR(Current_Pipeline_Amount__c &gt;0, Committed_ARR__c &gt;0), 1.25,1)))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Score Commercial Multiplier</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
